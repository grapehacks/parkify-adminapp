<UserControl x:Class="AdministrationPanel.Views.HistoryTab.HistoryTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdministrationPanel.Views.HistoryTab"
             xmlns:historyTab="clr-namespace:AdministrationPanel.ViewModels.HistoryTab;assembly=AdministrationPanel.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.Resources>
            <DataTemplate DataType="{x:Type historyTab:HistoryItemViewModel}" x:Key="HistoryItemTemplate">
                <Expander Header="{Binding DrawDate}" FlowDirection="RightToLeft">
                    <ListView ItemsSource="{Binding CardsCollection}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Card">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate DataType="{x:Type historyTab:HistoryItemCardsViewModel}">
                                            <TextBlock Text="{Binding Card}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Username" >
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate DataType="{x:Type historyTab:HistoryItemCardsViewModel}"> 
                                            <TextBlock Text="{Binding Username}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>

                            </GridView>
                        </ListView.View>
                    </ListView>
                </Expander>
            </DataTemplate>

        </Grid.Resources>
        
        <ListView ItemsSource="{Binding HistoryCollection}" >
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Data losowania" 
                                    CellTemplate="{StaticResource HistoryItemTemplate}">
                    </GridViewColumn>
                    
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
